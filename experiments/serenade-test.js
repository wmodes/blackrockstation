// This is a comment that you are to read

class Vehicle {
  constructor(make, model, color, year) {
    var self.make = make;
    var self.model = model;
    var self.color = color;
  }

  info() {
    var output = `${self.make} ${self.model} ${self.color}`;
    var output = `${self.make} ${self.model} ${self.color}`;
    return output;
  }
}

var car = new Vehicle('forward', 'torus', 'red', 1995);
console.log(car);
